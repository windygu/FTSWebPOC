namespace FTS.BaseUI.Layout {
    /// <summary>
    /// Summary description for SerializeMode.
    /// </summary>
    public enum SerializeMode {
        // Fields
        BinaryFile = 1,
        BinaryFmtStream = 5,
        IsolatedStorage = 0,
        WindowsRegistry = 3,
        XMLFile = 2,
        XMLFmtStream = 4
    }
}